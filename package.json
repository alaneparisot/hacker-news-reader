{
  "name": "hacker-news-reader",
  "version": "0.3.1",
  "description": "Hopefully, a more pleasant way to experience Hacker News.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "start-watch": "nodemon server/server.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && nyc --reporter=html --reporter=text mocha --exit server/test/**/*.js",
    "test-watch": "nodemon --exec \"npm test\"",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "author": "Alane Parisot",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.18.2",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.4.0",
    "mongodb": "^3.1.10",
    "mongoose": "^5.0.14",
    "supertest": "^3.3.0",
    "validator": "^10.9.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.0",
    "mocha": "^5.1.0",
    "moxios": "^0.4.0",
    "nodemon": "^1.17.3",
    "nyc": "^13.1.0"
  }
}
